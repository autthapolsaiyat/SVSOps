Include /etc/modsecurity.d/modsecurity.conf
Include /usr/local/owasp-modsecurity-crs/nginx-modsecurity.conf
Include /usr/local/owasp-modsecurity-crs/rules/*.conf

# ===== ตัวอย่าง whitelist (เปิดใช้เมื่อจำเป็น) =====
# SecRuleUpdateTargetById 942100 "!REQUEST_URI:/api/webhook/safe-endpoint"
# SecRuleUpdateTargetById 941100 "!REQUEST_URI:/api/upload"

# ===== โหมดรายงานเท่านั้นระหว่างทดสอบ =====
# SecRuleEngine DetectionOnly

